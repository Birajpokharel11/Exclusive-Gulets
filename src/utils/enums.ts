export enum Limits {
  BLOGS_PER_PAGE = 5,
  DESTINATIONS_PER_PAGE = 5
}

export enum BlogSort {
  sort_by = 'updated_at',
  sort_order = 'desc'
}
export enum DestinationSort {
  sort_by = 'updated_at',
  sort_order = 'desc'
}
